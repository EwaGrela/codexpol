# Copy to .env.production and fill in before deploying.
# All VITE_* vars are inlined at build time — never put secrets here.

# API endpoint for Skill Issue integration (leave empty until backend is live)
VITE_API_URL=
